[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = [
    "data/seed-reports.json",
    "data/**",
    "netlify/functions/seed-reports.json"
  ]
